import { ComaSeperatorPipe } from './coma-seperator.pipe';

describe('ComaSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ComaSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
